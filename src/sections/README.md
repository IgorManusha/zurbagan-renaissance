# `src/sections/`

Place large page sections here when they grow out of a single route file
(hero, pricing tables, FAQ accordions, etc.). They are imported by route
files in `src/routes/` and by reusable components in `src/components/`.
